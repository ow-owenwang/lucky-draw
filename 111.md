# 1. 清理旧的编译文件
rm -rf build

# 2. 针对 Electron 重新编译（arm64 架构）
npx electron -v  # 先查看 Electron 版本，假设输出是 v28.3.3

# 3. 使用 Electron 版本编译（替换为实际版本）
npx node-gyp rebuild --target=28.3.3 --arch=arm64 --disturl=https://electronjs.org/headers

# 或者直接运行我更新的脚本
npm run rebuild-electron



