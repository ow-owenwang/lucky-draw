appId: com.luckydraw.app
productName: 自动刷屏工具
directories:
  buildResources: build
  output: dist
files:
  - "**/*"
  - "!node_modules/**/*"
  - "node_modules/**/*"
  - "!build"
  - "build/Release/keyboard.node"
  - "native/**/*"
  - "binding.gyp"
# 强制使用 GitHub 官方源下载 Electron，避免镜像源问题
electronDownload:
  mirror: "https://github.com/electron/electron/releases/download/v"
# 禁用自动发布到 GitHub Release（使用 GitHub Actions 的 upload-artifact 代替）
publish: null
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: build/icon.ico
  # 自定义 Windows 安装程序文件名，移除 "Setup" 后缀
  artifactName: ${productName}-${version}-${arch}.${ext}
mac:
  target:
    - target: dmg
      arch:
        - arm64
  icon: build/icon.icns
  category: public.app-category.utilities
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true

